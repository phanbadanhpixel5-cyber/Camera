name: ip_camera_viewer
description: Ứng dụng xem camera IP RTSP
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_vlc_player: ^7.4.4
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  flutter_riverpod: ^2.5.1
  uuid: ^4.5.0
  permission_handler: ^11.3.1
  connectivity_plus: ^6.0.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  isar_generator: 3.0.0
  riverpod_generator: 2.3.0

dependency_overrides:
  analyzer: ^5.13.0
  source_gen: ^1.4.0

flutter:
  uses-material-design: true
